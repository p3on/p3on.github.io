---
title:  grep
os: linux
tags:
 - basic
 - linux
source: repo
---

## commands

Recursive search for files with the case insensitive `password` in it:

```bash
grep -E '*password*' -r -l -i # r: l: i:
```

